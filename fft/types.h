#ifndef TYPES_H
#define TYPES_H

#include <vector>
#include <unordered_map>
#include <map>

typedef std::map<double, long double> u_map_t;
typedef std::vector<std::pair<double, long double>> vec_t;

#endif // TYPES_H
